{
  "name": "nuriel",
  "version": "1.0.0",
  "private": true,
  "description": "ヌリエル — ペット写真→塗り絵線画AI変換サービス",
  "scripts": {
    "test": "vitest run --passWithNoTests",
    "test:watch": "vitest",
    "test:coverage": "vitest run --coverage"
  },
  "devDependencies": {
    "@vitest/coverage-v8": "^3.0.0",
    "vitest": "^3.0.0"
  }
}
